
# http://nokogiri.org/tutorials/installing_nokogiri.html
# these instructions seem to work

sudo apt-get install ruby1.8-dev ruby1.8 ri1.8 rdoc1.8 irb1.8
sudo apt-get install libreadline-ruby1.8 libruby1.8 libopenssl-ruby
sudo apt-get install libxslt-dev libxml2-dev
sudo gem install nokogiri

# these instructions did not seem complete enough
sudo apt-get install build-essential libopenssl-ruby ruby1.8-dev
sudo apt-get install gcc


sort -t, -n +3 -4 results.txt > sorted_results.txt

sort -t, -n +3 -4 sandstorm_raw.txt

